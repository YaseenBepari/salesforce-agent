{
  "name": "salesforce-copilot",
  "version": "1.0.0",
  "description": "AI-Powered Salesforce Copilot Sidebar Assistant",
  "scripts": {
    "backend": "python -m uvicorn backend.main:app --reload --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix frontend && pip install -r requirements.txt",
    "dev": "npx -y concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
